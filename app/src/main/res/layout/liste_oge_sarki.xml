<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="250dp"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:paddingVertical="4dp">

    <ImageView
        android:id="@+id/sarkiKapakImageView"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="centerCrop"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:src="@tools:sample/avatars" />

    <LinearLayout
        android:id="@+id/sarkiBilgiLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="@+id/sarkiKapakImageView"
        app:layout_constraintStart_toEndOf="@id/sarkiKapakImageView"
        app:layout_constraintTop_toTopOf="@+id/sarkiKapakImageView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0">

        <TextView
            android:id="@+id/sarkiAdiTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="Lovers on the Sun" />

        <TextView
            android:id="@+id/sarkiDetayTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#A78BFA"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="David Guetta • 387 Mn kez dinlendi" />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>